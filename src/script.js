var vidas = 5;
var limite = 10;
var numerosecreto = (Math.random() * (limite - 0) + 0);

const adivinhar = (numero) => {
    numero = parseInt(numero);
    if(numero >= 0 && numero <= limite) {

    } else {
        
    }
}
